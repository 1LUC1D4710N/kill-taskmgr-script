# kill-taskmgr-script
A PowerShell script that fixes the Windows 11 Task Manager background memory leak issue. Temporarily terminates Task Manager when idle in background until Microsoft releases an official patch.
